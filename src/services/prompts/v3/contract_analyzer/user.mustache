<contract_text>
{{{contract_text}}}
</contract_text>

<clause_index>
{{{clause_index}}}
</clause_index>

Work through EVERY entry in <clause_index> in order and apply the severity principles to each. Emit a risk_and_compliance_insights item for each clause that matches a Critical, High, or Medium principle, using the exact index entry as its clause_title, in clause-index order. Leave the list empty only if no clause matches. Produce the JSON object now.
