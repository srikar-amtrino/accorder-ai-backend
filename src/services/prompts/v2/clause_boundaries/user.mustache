Numbered document paragraphs:

{{{document}}}

Target indexes: {{{targets}}}

For each target index listed above, return the paragraph index at or nearest to it at which a new clause begins. Return only the structured result.
