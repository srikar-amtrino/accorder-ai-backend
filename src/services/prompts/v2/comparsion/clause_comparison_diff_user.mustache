The following are the changed clauses between the ORIGINAL and REVISED documents (unchanged clauses have been removed). Each region shows the full clause on each side plus the exact wording that changed within it.

{{{diff_digest}}}

Report every change across all regions. Set text_from_doc_a / text_from_doc_b to the FULL clause text (verbatim), not just the edited words. Return the JSON object now.