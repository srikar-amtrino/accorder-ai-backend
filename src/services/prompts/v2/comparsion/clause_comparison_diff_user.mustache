The following are the changed regions between the ORIGINAL and REVISED documents (unchanged text has been removed).

{{{diff_digest}}}

Report every change across all regions, following all requirements. Return the JSON object now.