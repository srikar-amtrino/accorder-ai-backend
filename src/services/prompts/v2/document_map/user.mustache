Document text:

{{{document}}}

Read the entire document above and build its DocumentMap. Extract exactly what the document contains, mark every item's confidence, flag anything genuinely ambiguous or missing, and return only the structured result.
