User request:
{{{user_query}}}

Draft the clause(s) needed to satisfy the request — one clause if a single provision is requested, or the full set of clauses for a complete agreement. Use [PLACEHOLDER] tokens for party names, dates, amounts, durations, and other facts the request does not supply.
