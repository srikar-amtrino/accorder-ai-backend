User request:
{{{user_query}}}

Use the document context below to ground the draft. Return as many clauses as the request needs — one clause if a single provision is requested, or the full set if a complete agreement is requested.

Document context:
{{{context}}}