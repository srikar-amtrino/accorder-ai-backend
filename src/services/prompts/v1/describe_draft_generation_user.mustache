<mode>{{mode}}</mode>

{{#has_agreement_type}}
<agreement_type>{{agreement_type}}</agreement_type>
{{/has_agreement_type}}

{{#has_document_context}}
<document_context>
The following is the full text of the document currently open. Read the parties, defined terms, and governing law directly from it, and ground your draft in them.
<document>
{{document_text}}
</document>
</document_context>
{{/has_document_context}}

{{^has_document_context}}
<no_document_mode>true</no_document_mode>
{{/has_document_context}}

<task>{{mode_instruction}}</task>

<user_request>
{{user_prompt}}
</user_request>

Produce the JSON object now per the `<output_schema>` for this mode. Return ONLY the JSON object — no prose, no markdown, no commentary.
</output>
